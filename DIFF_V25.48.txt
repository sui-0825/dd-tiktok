--- /dev/fd/63	2026-08-01 23:27:02.485508232 +0000
+++ /mnt/data/v2548_candidate/index.html	2026-08-01 23:27:02.289508228 +0000
@@ -2,7 +2,7 @@
 <html lang="ja">
 <head>
 <meta charset="utf-8">
-<meta name="dd-build" content="25.32-shared-save-foundation">
+<meta name="dd-build" content="25.48-annual-edit-clock-only">
 <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
 <meta name="theme-color" content="#f6f0e6">
 <meta name="application-name" content="D&D❀TikTok">
@@ -4195,7 +4195,7 @@
  if(!v){document.getElementById('annualEditError').textContent='入力値を入れてください';document.getElementById('annualEditError').classList.add('show');return}
  try{
   parseInput(v);const raw=normalizeRaw(v);
-  if(entry){entry.raw=raw;entry.inputRaw=raw}else{const timestamp=new Date(date+'T12:00:00').getTime()+db.entries.filter(e=>e.deviceId===deviceId&&e.date===date).length;db.entries.push({id:uid(),deviceId,date,month:date.slice(0,7),year:date.slice(0,4),raw,inputRaw:raw,timestamp,memo:''})}
+  if(entry){entry.raw=raw;entry.inputRaw=raw;entry.editedAt=Date.now()}else{const timestamp=new Date(date+'T12:00:00').getTime()+db.entries.filter(e=>e.deviceId===deviceId&&e.date===date).length;db.entries.push({id:uid(),deviceId,date,month:date.slice(0,7),year:date.slice(0,4),raw,inputRaw:raw,timestamp,editedAt:Date.now(),memo:''})}
   annualLastSavedCell=`${date}|${deviceId}`;recalcDevice(deviceId);persist();closeAnnualEditor();renderAnnual();renderHome();toast(entry?'修正日以降、この端末だけ再計算しました':'年間データへ直接登録しました');
  }catch(e){const err=document.getElementById('annualEditError');err.textContent=e.message||'入力形式を確認してください';err.classList.add('show')}
 }
@@ -5422,7 +5422,7 @@
 <div id="cloudStatusPill" data-mode="local">端末内保存</div>
 
 <script src="./backend-config.js?v=2515"></script>
-<script src="./app-cloud-bridge.js?v=2542"></script>
+<script src="./app-cloud-bridge.js?v=2548"></script>
 <script>
 // Ver23.0: GitHub Pagesで更新を確実に反映するため、旧Service Workerとキャッシュを解除します。
 if ('serviceWorker' in navigator) {
@@ -5459,7 +5459,7 @@
   removeOldRecovery();
   document.addEventListener('DOMContentLoaded', removeOldRecovery, {once:true});
   const badge=document.createElement('div');
-  badge.id='ddBuildBadge'; badge.textContent='Ver25.44';
+  badge.id='ddBuildBadge'; badge.textContent='Ver25.48';
   badge.style.cssText='position:fixed;left:8px;bottom:8px;z-index:40000;background:#173c55;color:#fff;border-radius:999px;padding:4px 8px;font:800 10px system-ui;opacity:.82;pointer-events:none';
   document.addEventListener('DOMContentLoaded',()=>document.body.appendChild(badge),{once:true});
 })();
